apiVersion: v1
kind: Service
metadata:
  name: {{ include "infrafi-web.fullname" . }}
  labels:
    {{- include "infrafi-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "infrafi-web.selectorLabels" . | nindent 4 }}